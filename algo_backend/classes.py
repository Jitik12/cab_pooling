class Person:
    min: int
    max: int
    num_people: int
    user_id: int
    def __init__(self, min, max, num_people, user_id):
        self.min = min
        self.max = max
        self.num_people = num_people
        self.user_id = user_id


